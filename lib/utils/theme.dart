import 'package:flutter/material.dart';

class AppTheme {
  static final lightMode = ThemeData(colorScheme: ColorScheme.light(background: Colors.white));

  static final darkMode = ThemeData(colorScheme: ColorScheme.dark(background: Colors.grey.shade900));
}
